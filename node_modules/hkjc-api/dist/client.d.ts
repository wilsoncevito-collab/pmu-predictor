export declare class HKJCClient {
    private client;
    constructor(endpoint?: string);
    request<T>(query: string, variables?: Record<string, any>): Promise<T>;
}
export declare const defaultClient: HKJCClient;
